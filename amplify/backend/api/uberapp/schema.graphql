type User @model {
  id: ID!
  username: String!
  email: String!
}

type Car @model {
  id: ID!
  type: String!
  latitude: Float,
  longitude: Float,
  heading: Float,
}

type Order @model {
  id: ID!
}
